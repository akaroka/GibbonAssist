# Architecture Brief: 录音转文字工具

## 1. 硬性约束（Hard Constraints）

| 维度 | 约束值 | 优先级 | 来源 |
|------|--------|--------|------|
| 最大体积 | < 10MB（目标），< 15MB（可接受） | P0 | PRD NFR + 用户确认 |
| 外部依赖 | 无运行时依赖，单文件可执行 | P0 | 用户确认 |
| 目标平台 | Windows 10+, macOS 10.15+ | P0 | PRD |
| 启动时间 | < 2s（界面优先显示） | P1 | 用户确认 |
| 内存上限 | < 500MB | P1 | PRD NFR |
| 离线能力 | 转写可离线，润色需联网 | P2 | PRD NFR |

## 2. 推荐技术栈

### Primary Stack

| 组件 | 技术选择 | 理由 |
|------|----------|------|
| **编程语言** | **Go** | 编译为单二进制，跨平台原生支持，标准库丰富，启动快 |
| **GUI 框架** | **Fyne** / **Wails (Go)** | Fyne 依赖少量 CGO（glfw），单文件 ~5-8MB；Wails 体积小但依赖 WebView |
| **音频录制** | **Windows**: WASAPI (winmm) <br>**macOS**: CoreAudio | 原生 API，零依赖，体积最小 |
| **转写引擎** | **whisper.cpp** (Go bindings) | C++ 实现，性能优，Go 绑定成熟，支持 base 模型 |
| **热键监听** | **Windows**: RegisterHotKey <br>**macOS**: NSEvent/Quartz | 原生实现，响应快 |
| **HTTP 客户端** | Go `net/http` | 标准库，无依赖 |
| **配置存储** | JSON 文件 | 简单，无依赖 |

### 备选方案对比

| 方案 | 体积估算 | 启动时间 | 跨平台 | 拒绝原因 |
|------|----------|----------|--------|----------|
| **Rust + egui** | ~8MB | < 1s | ✅ | 学习曲线陡峭，开发效率低 |
| **Rust + Tauri** | ~15MB+ | ~2s | ✅ | 依赖 WebView，启动慢 |
| **Python + PyQt** | 需运行时 | - | ✅ | 违反"无外部依赖"P0 约束 |
| **Electron** | >100MB | >3s | ✅ | 违反"<15MB"P0 约束 |
| **C++ + Qt** | ~20MB+ | < 1s | ⚠️ 复杂 | 体积超标，编译复杂 |

## 3. 架构设计

### 3.1 模块结构

```
┌─────────────────────────────────────────────────────────┐
│                      Main (Go)                          │
├─────────────┬─────────────┬─────────────┬───────────────┤
│   UI Layer  │  Audio Rec  │ Transcribe  │    LLM        │
│   (Fyne)    │  (Native)   │(whisper.cpp)│  (HTTP)       │
├─────────────┴─────────────┴─────────────┴───────────────┤
│              Config Manager (JSON)                      │
├─────────────────────────────────────────────────────────┤
│              Hotkey Manager (Native)                    │
└─────────────────────────────────────────────────────────┘
```

### 3.2 启动流程（异步加载）

```
用户双击
    ↓
┌─────────────┐     ┌─────────────────┐
│  显示 UI    │────→│ 后台加载引擎    │
│  (<100ms)   │     │ (whisper.cpp)   │
└─────────────┘     └─────────────────┘
    ↓                       ↓
┌─────────────┐     ┌─────────────────┐
│ 检查模型    │     │ 引擎就绪        │
│ 是否存在    │     │ (内存中)        │
└─────────────┘     └─────────────────┘
    ↓
模型存在? ──是──→ 正常使用
    ↓ 否
┌─────────────────────────────────────┐
│ 弹窗: "需要下载转写模型 (~74MB)"    │
│ [自动下载] [自行下载]               │
└─────────────────────────────────────┘
```

### 3.3 运行时内存模型

| 阶段 | 内存占用 | 说明 |
|------|----------|------|
| 启动后 | ~20MB | UI + 基础运行时 |
| 引擎加载后 | ~100MB | + whisper.cpp base 模型 |
| 录音中 | ~120MB | + 音频缓冲区 |
| 转写中 | ~200MB | + 推理内存峰值 |
| 空闲 | ~100MB | 保持引擎常驻 |

## 4. 被拒绝的方案

| 方案 | 拒绝原因 |
|------|----------|
| **Tauri** | WebView 依赖增加体积，启动时间 >2s |
| **Electron** | 体积 >100MB，严重违反 P0 约束 |
| **Python** | 需要运行时，违反"单文件可执行"约束 |
| **Rust + egui** | 学习成本高，开发效率低（备选可行） |
| **C++ + Qt** | 编译复杂，静态链接后体积 >20MB |

## 5. 架构决策记录（ADR）

### ADR-001: 选择 Go 而非 Rust

**背景**: 需要单二进制、<15MB、跨平台、快速开发

**决策**: 使用 Go + Fyne 而非 Rust + egui

**理由**:
- Go 编译速度更快，开发迭代效率高
- Fyne 依赖少量 CGO（glfw），但不需要外部 C 库，Windows 上编译后可自包含
- 团队熟悉 Go，维护成本低
- 体积与 Rust 方案相当（~8-12MB）

**后果**:
- 性能略低于 Rust，但满足需求
- GUI 美观度不如 egui，但满足功能需求

### ADR-002: 模型首次运行时下载

**背景**: base 模型 74MB，无法打包进 15MB 安装包

**决策**: 首次启动弹窗，用户选择后下载；按钮上方显示进度

**理由**:
- 遵守体积约束
- 用户有知情权（弹窗提示）
- 支持"自行下载"模式，适应离线环境
- 国内用户提供 ModelScope 镜像

**模型路径与命名**:
- 路径: `./models/ggml-model.bin`
- 统一命名，用户可替换为更大模型（tiny/base/small）

**下载流程**:
```
首次启动 → 检查模型 → 弹窗选择 → [自动下载: 按钮上方显示进度]
                                      [自行下载: 显示操作步骤]
```

**后果**:
- 首次使用需要网络
- 需要处理下载失败场景：下次启动检测到模型不存在，重新弹窗提示下载
- 需要维护国内镜像地址

### ADR-003: 原生音频 API 而非跨平台库

**背景**: 需要零依赖、最小体积

**决策**: Windows WASAPI + macOS CoreAudio，条件编译

**理由**:
- 零外部依赖
- 延迟最低
- 代码量可控（录音功能简单）

**后果**:
- 平台特定代码需要分别测试
- 增加少量 #ifdef 条件编译

### ADR-004: 热键防抖策略

**背景**: 用户可能快速连击热键，硬件连击或软件重复触发导致状态错乱

**决策**: 状态机驱动 + 300ms 防抖窗口

**理由**:
- 状态已定义（空闲→录音→处理→空闲），连击本质是非法状态转换
- 300ms 窗口覆盖硬件连击和大部分误触
- ESC 键例外，录音中可随时取消（不进入防抖）

**处理逻辑**:
```
空闲 + 热键 → 录音中（开始计时）
录音中 + 热键 → 处理中（停止录音）
处理中 + 热键 → 忽略
< 1秒内连续热键 → 忽略（录音太短，丢弃）
```

**后果**:
- 极端场景下用户需等待 300ms 才能响应，但感知极低
- 需在前端维护状态机，增加少量代码

## 6. 风险与缓解

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| Fyne 在 macOS 表现不佳 | 中 | 高 | 预留 Wails 作为备选方案 |
| whisper.cpp Go 绑定不稳定 | 低 | 高 | 直接使用 CGO 调用 C++ 库 |
| 模型下载失败/慢 | 中 | 中 | 下载失败则放弃，下次启动重新提示下载 |
| 热键与系统冲突 | 高 | 低 | 启动检测，提供配置修改 |
| 热键连击误触发 | 高 | 低 | 状态机 + 300ms 防抖窗口 |
| 跨平台编译复杂 | 中 | 中 | GitHub Actions 自动化构建 |

## 7. 技术栈验证清单

开发前需要验证：

- [ ] **PoC-01**: Go + Fyne 编译 Hello World，确认 Windows/macOS 体积 < 15MB
- [ ] **PoC-02**: whisper.cpp Go 绑定，确认 base 模型加载后内存 < 200MB
- [ ] **PoC-03**: Windows WASAPI 录音，确认无额外 DLL 依赖
- [ ] **PoC-04**: macOS CoreAudio 录音，确认沙盒兼容
- [ ] **PoC-05**: 热键注册（Win + Mac），确认响应延迟 < 50ms

## 8. 已确认决策

| 问题 | 决策 |
|------|------|
| GUI 框架 | **Fyne**（Wails 作为 macOS 版本备选） |
| 代码签名 | MVP 阶段暂不考虑 |
| 剪贴板粘贴 | 引入平台特定代码实现 `Ctrl+V` 模拟（粘贴前自动点击聚焦） |
| 配置生效规则 | 一般配置即时生效；热键/模型路径修改需重启 |

## 9. 已确认决策补充

| 问题 | 决策 |
|------|------|
| Fyne 视觉风格 | 接受默认风格，无需自定义主题 |
| 模型下载源 | **ModelScope 国内镜像为主**，HuggingFace 作为备用 |

---

*Architecture Brief Version: 1.0*  
*Created: 2026-04-17*  
*Status: 待 PoC 验证*

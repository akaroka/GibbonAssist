# PRD: 录音转写工具

## 1. 概述

| 项目 | 内容 |
|------|------|
| **目标用户** | 开发者、内容创作者、需要语音输入效率的桌面用户 |
| **核心目标** | 提供极简、低延迟的语音转文字体验，支持中英文混杂 |
| **成功标准** | 从点击到文字粘贴完成 < 10 秒（1 分钟录音） |
| **平台范围** | Windows 10+、macOS 10.15+ |

## 2. 用户故事

- **US-01**: As a 开发者，I want 按热键快速录音转文字，so that 不打断编码思路流
- **US-02**: As a 内容创作者，I want 去除语气词和停顿的润色文本，so that 减少后期编辑时间
- **US-03**: As a 多语言用户，I want 中英文混杂识别，so that 自然表达专业术语
- **US-04**: As a 效率工具用户，I want 一键操作完成全流程，so that 无需手动复制粘贴

## 3. 功能规格

### 3.1 主流程

```
                         ┌─────────┐
                    ┌───→│  取消   │
                    │ESC │ (丢弃)  │
                    │    └─────────┘
                    │         ↑
┌─────────┐    点击/热键     ┌─────────┐    再次点击/热键    ┌─────────┐
│  空闲   │ ──────────────→ │  录音中  │ ────────────────→ │  处理中  │
│ (灰色)  │                 │ (红色)  │                    │ (蓝色)  │
└─────────┘                 └────┬────┘                   └─────────┘
     ↑                           │                            │
     │                           │ 超时>10分钟                  │ 粘贴完成
     │                           ▼                            │
     │                    ┌─────────┐                         │
     └────────────────────│  丢弃   │                         │
                          │ (静默)  │←────────────────────────┘
                          └─────────┘
```

**时长分支说明**：
- **< 1 秒**：丢弃录音，提示"录音太短"
- **≥ 1 秒且 < 2 分钟**：直接进入处理中
- **2-10 分钟**：弹出提示"转写可能需要一些时间"，无需确认，自动进入处理中
- **> 10 分钟**：自动丢弃，静默恢复空闲状态

**详细步骤**：
1. 用户选择麦克风设备（下拉框）
2. 用户点击按钮或按下热键 → 进入"录音中"状态（红色）
3. **录音中按 ESC 键** → 取消录音，丢弃音频，恢复"空闲"状态
4. **录音时长 > 10 分钟** → 自动丢弃录音，静默恢复"空闲"状态（用户可能忘记关闭）
5. 用户再次点击/热键 → 停止录音
6. **录音时长 2-10 分钟** → 弹出提示："录音时间较长，转写可能需要一些时间"（仅提示，无需确认）
7. 进入"处理中"状态（蓝色）→ 执行转写 + LLM 润色
8. 处理完成 → **自动点击光标位置确保聚焦** → 粘贴文字到当前输入框 → 复制到剪贴板
9. 恢复"空闲"状态（灰色）

**音频处理**：
- `recording.save_audio = false`（默认）：录音仅保存在内存，处理完成后直接丢弃
- `recording.save_audio = true`：保存为 WAV 文件到指定路径，供用户回溯

**首次启动模型下载流程**：
```
首次启动
    ↓
检查模型文件是否存在 (./models/ggml-model.bin)
    ↓ 不存在
┌─────────────────────────────────────┐
│ 需要下载转写模型 (~74MB)            │
│                                     │
│ 来源：HuggingFace / ModelScope(镜像)│
│ 路径：[程序目录]\models\             │
│                                     │
│ [自动下载]      [自行下载]          │
└─────────────────────────────────────┘
    ↓ 自动下载                    ↓ 自行下载
┌─────────────────────────┐    ┌─────────────────────────────┐
│ 按钮上方显示进度：       │    │ 请按以下步骤操作：           │
│ "下载中... 45%"         │    │ 1. 下载模型文件              │
│                         │    │    [打开 HuggingFace]        │
│ [取消下载]              │    │    [打开 ModelScope 镜像]    │
└─────────────────────────┘    │ 2. 重命名为 ggml-model.bin   │
    ↓ 完成                      │ 3. 放置到程序目录 models 文件夹│
正常使用                        │                             │
                                │ [已完成，开始使用]           │
                                └─────────────────────────────┘
```

**下载失败处理**：
- 网络失败/超时 → 弹窗提示："下载失败，请检查网络或选择自行下载"
- 提供两个按钮：[重试下载] [自行下载]
- 自行下载流程同上

### 3.2 界面元素

| 元素 | 类型 | 位置 | 行为 | 状态 |
|------|------|------|------|------|
| 设备选择 | 下拉框 | 顶部 | 列出可用麦克风；无设备时显示"未检测到麦克风"并禁用 | 始终可交互 |
| 录音按钮 | 按钮 | 中部 | 触发录音/停止 | 灰→红→蓝→灰 |
| 设置入口 | 图标/菜单 | 右上角 | 打开配置窗口 | 空闲时可点击 |

**按钮状态样式**：
- **空闲**: 灰色背景，文本"开始录音"
- **录音中**: 红色背景，文本动态切换："录音中." → "录音中.." → "录音中..."（每 500ms 切换，无动画）
- **处理中**: 蓝色背景，文本动态切换："处理中." → "处理中.." → "处理中..."（每 500ms 切换，无动画）

### 3.3 配置功能

**配置界面元素与 JSON 字段映射**：

| 配置区域 | 界面元素 | JSON 字段 | 类型 | 说明 |
|----------|----------|-----------|------|------|
| **热键设置** | 热键 | `hotkey` | 下拉框+输入框 | 选择预设热键或直接输入自定义组合 |
| | 冲突提示 | - | 标签 | 红色文字提示"该热键已被系统占用" |
| **输出设置** | 自动粘贴 | `output.auto_paste` | 开关 | 转写后自动粘贴到光标位置 |
| | 复制到剪贴板 | `output.copy_to_clipboard` | 开关 | 转写后复制到系统剪贴板 |
| | 自动点击聚焦 | `output.auto_click_focus` | 开关 | 粘贴前自动点击光标位置确保聚焦 |
| **录音设置** | 保存音频文件 | `recording.save_audio` | 开关 | 是否保存 WAV 文件到本地 |
| | 音频保存路径 | `recording.audio_path` | 输入框 | WAV 文件保存目录，留空使用默认路径 |
| | 长录音阈值 | `recording.long_threshold` | 数字 | 提示阈值（秒），默认 120 |
| | 丢弃阈值 | `recording.discard_threshold` | 数字 | 超长丢弃阈值（秒），默认 600 |
| **转写设置** | 引擎 | `transcription.engine` | 只读 | 转写引擎，固定 "whisper.cpp" |
| | 模型 | `transcription.model` | 下拉框 | 模型大小（tiny/base/small），默认 "base" |
| | 语言 | `transcription.language` | 下拉框 | 识别语言，默认 "auto" |
| **模型下载** | 模型路径 | `model.path` | 输入框 | 模型文件路径，默认 "./models/ggml-model.bin" |
| | 下载地址 | `model.download_url` | 输入框 | 主下载源（ModelScope 国内镜像） |
| | 备用地址 | `model.mirror_url` | 输入框 | 备用源（HuggingFace） |
| | 自动下载 | `model.auto_download` | 开关 | 是否自动下载模型 |
| **大模型设置** | LLM URL | `llm.url` | 输入框 | API 地址，留空则跳过润色 |
| | LLM Key | `llm.key` | 密码框 | API Key，留空则跳过润色 |
| | 超时时间 | `llm.timeout` | 数字输入 | 请求超时（秒），默认 10 |
| | 提示词 | `llm.prompt` | 文本框 | 润色提示词 |
| | 测试连接 | - | 按钮 | 验证 LLM 配置是否可用 |
| **界面设置** | 语言 | `ui.language` | 下拉框 | "auto"/"zh"/"en"，默认 "auto" |

### 3.4 热键防抖

热键快速连击防护（300ms 防抖窗口）：

| 当前状态 | 收到热键 | 行为 |
|----------|----------|------|
| 空闲 | 点击 | 启动录音 |
| 录音中 | 点击 | 停止录音，进入处理中 |
| 处理中 | 点击 | 忽略（按钮不响应） |
| 录音中 < 1 秒后连续点击 | 点击 | 忽略（录音太短，不产生输出） |

**实现逻辑**：
- 状态转换后立即进入 300ms 防抖窗口
- 窗口内所有热键事件被丢弃
- ESC 键不受防抖限制（录音中可随时取消）

### 3.5 错误处理

| 错误场景 | 系统行为 | 用户提示 | Win 效果 | Mac 效果 |
|----------|----------|----------|----------|----------|
| 热键冲突 | 启动时检测，阻止注册 | 弹窗："热键 X 已被占用，请在设置中更换" | Win32 MessageBox | NSAlert 模态框 |
| 麦克风被占用 | 录音启动失败 | Toast："麦克风被其他应用占用" | 系统通知中心 | 系统通知中心 |
| 无麦克风设备 | 启动时检测，禁用录音按钮 | 下拉框显示"未检测到麦克风"，按钮置灰不可点击 | - | - |
| 热键防抖触发 | 处理中忽略热键 | 无提示，按钮保持当前状态 | - | - |
| 转写服务失败 | 重试 1 次，失败后回退 | Toast："转写失败，请检查网络后重试" | 系统通知中心 | 系统通知中心 |
| 光标未聚焦 | 粘贴失败，保留剪贴板 | 静默，用户可用 Ctrl+V 手动粘贴 | - | - |
| 录音文件损坏 | 丢弃录音，记录日志 | Toast："录音异常，请重试" | 系统通知中心 | 系统通知中心 |
| 录音超时(>10min) | 丢弃音频，清理临时文件 | **无提示，静默恢复空闲状态** | - | - |
| LLM 未配置 | 跳过润色，直接返回原文 | 无提示 | - | - |
| LLM 润色超时 | 返回原始转写文本 | Toast："润色超时，已返回原文" | 系统通知中心 | 系统通知中心 |
| LLM 网络失败 | 超时后返回原始转写文本 | Toast："网络异常，已返回原文" | 系统通知中心 | 系统通知中心 |
| LLM 404/403 | 返回原始转写文本 | Toast："LLM 服务异常(404/403)，已返回原文" | 系统通知中心 | 系统通知中心 |
| 用户取消录音 | 丢弃音频，清理临时文件 | 无提示，静默恢复空闲状态 | - | - |

## 4. 边界条件

| 条件 | 限制值 | 处理方式 |
|------|--------|----------|
| 超长录音丢弃 | > 10 分钟 | 自动丢弃，静默恢复空闲状态 |
| 长录音提示 | 2-10 分钟 | 停止时提示"转写可能需要一些时间"（无需确认）|
| 最小录音时长 | 1 秒 | 丢弃，提示"录音太短" |
| 转写超时 | 30 秒 | 返回原文，提示超时 |
| LLM 润色超时 | 10 秒 | 返回原文，提示超时 |
| 单文件大小上限 | 50MB | 拒绝处理，提示"录音文件过大" |

## 5. 验收标准（AC）

### 功能验收
- [ ] **AC-01**: 点击按钮从空闲到录音中状态切换 < 100ms
- [ ] **AC-02**: 录音中按钮显示红色，文本动态切换"录音中." / ".." / "..."
- [ ] **AC-03**: 处理中按钮显示蓝色，文本动态切换"处理中." / ".." / "..."
- [ ] **AC-04**: 录音 2-10 分钟时停止，弹出提示（无需确认）
- [ ] **AC-05**: 转写结果自动粘贴到当前光标位置（可选自动点击聚焦）
- [ ] **AC-06**: 转写结果同时写入系统剪贴板
- [ ] **AC-06a**: 支持配置是否保存音频 WAV 文件
- [ ] **AC-07**: 热键冲突时启动检测并弹窗提示
- [ ] **AC-08**: 支持 JSON 配置热键，支持预设下拉框选择
- [ ] **AC-08a**: 录音中按 ESC 键取消，恢复空闲状态，不产生任何输出
- [ ] **AC-08b**: 录音超过 10 分钟自动丢弃，静默恢复空闲状态
- [ ] **AC-08c**: LLM 未配置时跳过润色，直接返回原文
- [ ] **AC-08d**: LLM 404/403/超时/网络失败时，提示并返回原文
- [ ] **AC-08e**: UI 根据系统语言自动切换中/英文

### 性能验收
- [ ] **AC-09**: 应用冷启动时间 < 2 秒
- [ ] **AC-10**: 1 分钟录音的端到端处理时间 < 10 秒
- [ ] **AC-11**: UI 响应延迟 < 50ms（按钮点击反馈）

### 兼容性验收
- [ ] **AC-12**: Windows 10/11 正常运行
- [ ] **AC-13**: macOS 10.15+ 正常运行
- [ ] **AC-14**: 支持中英文混杂识别准确率 > 85%

## 6. 非功能性需求（NFR）

| 维度 | 要求 | 优先级 |
|------|------|--------|
| 安装包体积 | **< 10MB（目标）**，< 15MB（可接受） | P0 |
| 内存占用 | **< 200MB（目标）**，< 500MB（上限） | P1 |
| 离线能力 | 转写可离线（本地模型），润色需联网 | P2 |
| 隐私保护 | 录音文件本地处理，不上传原始音频 | P0 |
| 无障碍 | 支持键盘全程操作 | P2 |

## 7. 已确认决策

| 问题 | 决策 |
|------|------|
| **Q1**: LLM 润色服务 | 通过配置文件 `llm.url` + `llm.key` 自定义；未配置时跳过润色；404/403 错误时提示并返回原文 |
| **Q2**: 录音历史记录 | ❌ 不做 |
| **Q3**: 转写引擎 | whisper.cpp |
| **Q4**: 多语言界面 | ✅ 支持中/英文，自动读取系统语言设置 |

## 8. 附录

### 8.1 状态机图

```
                    ┌─────────────┐
         ┌─────────│   初始化    │
         │         └──────┬──────┘
         │                │ 启动完成
         │                ▼
         │         ┌─────────────┐
         │    ┌───│    空闲     │◄────────────────┐
         │    │   │  (灰色按钮)  │                 │
         │    │   └──────┬──────┘                 │
         │    │          │ 点击/热键               │
         │    │          ▼                        │ 粘贴完成
         │    │   ┌─────────────┐    ESC取消      │
         │    └──►│   录音中    │────────┐        │
         │        │  (红色按钮)  │        │        │
         │        └──────┬──────┘        ▼        │
         │               │ 再次点击/   ┌─────────┐│
         │               │ 热键        │  取消   ││
         │               ▼             │ (丢弃)  ││
         │   ┌─────────────────────┐   └─────────┘│
         │   │ 时长>10分钟? → 丢弃  │─────────────┘
         │   │ 时长2-10分钟? → 提示  │
         │   │ 时长≥1秒且<2分钟? → 直接处理 │
         └───┤ 时长<1秒? → 丢弃+提示 │
             └──────────┬──────────┘
                        │ （提示仅展示，不阻塞）
                        ▼
                 ┌─────────────┐
                 │   处理中    │
                 │  (蓝色按钮)  │
                 └─────────────┘
```

**状态流转说明**：
- **录音中 → 处理中**：用户再次点击/热键停止录音
- **录音中 → 取消**：录音中按 ESC，或时长 < 1 秒
- **录音中 → 丢弃**：时长 > 10 分钟，自动丢弃
- **提示框**：时长 2-10 分钟时弹出提示，仅展示信息，不阻塞流程

### 8.2 配置文件示例

```json
{
  "hotkey": "Ctrl+Shift+R",
  "output": {
    "auto_paste": true,
    "copy_to_clipboard": true,
    "auto_click_focus": true
  },
  "recording": {
    "save_audio": false,
    "audio_path": "",
    "long_threshold": 120,
    "discard_threshold": 600
  },
  "transcription": {
    "engine": "whisper.cpp",
    "model": "base",
    "language": "auto"
  },
  "model": {
    "path": "./models/ggml-model.bin",
    "download_url": "https://modelscope.cn/models/...",
    "mirror_url": "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin",
    "auto_download": true
  },
  "llm": {
    "url": "https://api.openai.com/v1/chat/completions",
    "key": "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "timeout": 10,
    "prompt": "请润色以下文本，去除语气词和冗余停顿，保持原意："
  },
  "ui": {
    "language": "auto"
  }
}
```

**配置说明**：
- `hotkey`: 当前生效的热键组合（UI 提供预设选择，也可直接编辑）
- `output.auto_paste`: 转写后自动粘贴到光标位置（需配合 `auto_click_focus`）
- `output.copy_to_clipboard`: 转写后复制到系统剪贴板
- `output.auto_click_focus`: 粘贴前自动点击光标位置确保聚焦（平台特定代码实现 Ctrl+V）
- `llm.url` / `llm.key`: 大模型 API 配置，**留空时跳过润色**，直接返回转写原文
- `llm.timeout`: 润色请求超时时间（秒）
- `ui.language`: `auto` 自动检测系统语言，`zh` 强制中文，`en` 强制英文

**配置生效规则**：
- 一般配置（输出设置、录音设置、LLM 设置）：**即时生效**
- 热键修改：**重启后生效**
- 模型路径修改：**重启后生效**（需重新加载引擎）

---

*PRD Version: 2.0*  
*Created: 2026-04-17*  
*Status: 已评审*
